{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "c=$(jq -r '.tool_input.command // \"\"'); printf '%s' \"$c\" | grep -qE -- '(^|[[:space:]])--no-verify([[:space:]]|$)|(^|[[:space:]])HUSKY=0([[:space:]]|$)|HUSKY_SKIP_HOOKS|core\\.hooksPath=' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: this repo does not allow bypassing git hooks. pre-commit (prettier + lint) and pre-push (content build, tests, lint, baseline drift) are the quality gate. If a hook fails, fix the cause or report it — never skip it.\"}}' || true"
          }
        ]
      }
    ]
  }
}
